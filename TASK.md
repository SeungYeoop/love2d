# TASK

## 현재 목표
- 이 저장소를 "짧은 설명 + 짧은 명령"만으로도 고품질 LÖVE2D 프로토타입을 빠르게 만드는 템플릿으로 고도화한다.
- AI가 규칙을 지키며 구조 있는 playable prototype부터 만들고, 다음 개선 제안까지 이어가게 만든다.
- 작업 완료 시 LÖVE2D를 바로 다시 실행하거나 자동 재실행 흐름을 제공한다.
- 처음 들어온 사용자가 바로 사용할 수 있는 튜토리얼형 `README.md`를 유지한다.

## 이번 작업 범위
- AI 작업 루프를 문서화
- `prompts/` 템플릿을 첫 구현용 / 반복 개선용 중심으로 개선
- Windows용 LÖVE2D 재실행 및 watcher 스크립트 추가
- 현재 구조 설명과 세션 요약을 템플릿 관점으로 갱신

## 이번 작업에서 하지 않는 것
- 현재 게임 로직 자체의 대규모 리팩토링
- 특정 장르 전용 시스템 추가
- 외부 라이브러리 추가
- 사용자가 요청하지 않은 대규모 폴더 구조 강제 적용

## 현재 파악한 상태
- 루트 기준 확인된 코드 파일은 `main.lua` 하나다.
- 내용상 `love.*` API를 사용하므로 Lua + LÖVE 기반 프로젝트로 본다.
- 현재는 매우 작은 시작 상태이며, 템플릿용 프로젝트로 사용하려는 의도가 명확하다.
- 문서형 템플릿은 기본 형태가 갖춰져 있고, 이제 제작 루프와 실행 경험을 강화하는 단계다.

## 바로 다음에 이어질 수 있는 작업
1. 사용자 선호에 맞는 기본 게임 스캐폴드(`conf.lua`, `src/`, `scenes/`) 추가
2. 프로토타입 기본 품질 바를 만족하는 예제 vertical slice 추가
3. LÖVE 실행 검증 루프를 더 자동화할지 결정

## TODO
- 선호 템플릿 범위 확정: 문서형 / 구조형 / 예제 포함형
- 기본 장면 구조 필요 여부 확인
- 예제 코드가 필요한지 확인
- watcher 외에 hot reload 성격의 흐름이 필요한지 확인
